{
  "name": "sample airport info",
  "version": "1.0.0",
  "description": "Airport info skill",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "alexa-sdk": "^1.0.0",
    "axios": "^0.18.0"
  }
}
